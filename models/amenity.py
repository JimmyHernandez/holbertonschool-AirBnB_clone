#!/usr/bin/python3
"""The class 'Amenity' inherits from the `BaseModel` class."""
from models.base_model import BaseModel


class Amenity(BaseModel):
    """Creating a public class attribute called name."""

    name = ""
